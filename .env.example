GEMINI_API_KEY=your_gemini_api_key

# true = cheap development model only
# false = production model fallback chain
GEMINI_DEV_MODE=true

# Used only when GEMINI_DEV_MODE=true
GEMINI_DEV_MODEL=gemini-3.5-flash-lite
